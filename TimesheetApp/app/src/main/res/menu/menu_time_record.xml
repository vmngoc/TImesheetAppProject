<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools" tools:context=".TimeRecordActivity">

    <!-- "Add new time entry", should appear as action button if possible -->
    <item
        android:id="@+id/action_add_new_entry"
        android:icon="@drawable/ic_add_white_24dp"
        android:title="@string/action_add_new_entry"
        app:showAsAction="ifRoom"/>

    <!-- Settings, should always be in the overflow -->
    <item android:id="@+id/action_settings"
        android:title="@string/action_settings"
        app:showAsAction="never"/>

</menu>